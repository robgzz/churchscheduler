# Upgrade to V5.1.0

No new Azure tables or resources are required. Deploy the full application through the existing GitHub Actions pipeline.

The workflow changes the scheduler job cadence to every 15 minutes. No bootstrap is required.

After deployment, close and reopen the browser/PWA once so V5.1 static assets replace older cached assets.
